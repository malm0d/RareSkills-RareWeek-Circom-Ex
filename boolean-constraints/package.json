{
  "name": "circom-multiplier",
  "version": "1.0.0",
  "description": "Basic Circom multiplier circuit with tests",
  "scripts": {
    "compile": "circom multiplier.circom --r1cs --wasm --sym -o multiplier_js",
    "test": "mocha multiplier.test.js"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "snarkjs": "^0.7.0"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  }
}
