{
  "name": "bitwise-nand-circuit",
  "version": "1.0.0",
  "scripts": {
    "compile": "circom bitwise_nand.circom --r1cs --wasm --sym --output bitwise_nand_js",
    "test": "mocha bitwise_nand.test.js"
  }
}