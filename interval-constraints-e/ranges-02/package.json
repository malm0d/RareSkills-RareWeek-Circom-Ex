{
  "name": "excluded-intervals-circuit",
  "version": "1.0.0",
  "scripts": {
    "compile": "circom excluded_intervals.circom --r1cs --wasm --sym --output excluded_intervals_js",
    "test": "mocha excluded_intervals.test.js"
  }
}